## USACO 2021 February Contest, Gold

| Gold 题目          | 前置题                                                       | 主要训练内容                |
| ------------------ | ------------------------------------------------------------ | --------------------------- |
| **Modern Art 3**   | [P1880 石子合并](https://www.luogu.com.cn/problem/P1880)     | 基础区间 DP、枚举分割点     |
|                    | [P4170 涂色](https://www.luogu.com.cn/problem/P4170)         | 覆盖型区间 DP、相同颜色合并 |
| **Stone Game**     | [P1403 约数研究](https://www.luogu.com.cn/problem/P1403)     | 调和级数、整除求和          |
|                    | [CSES Common Divisors](https://cses.fi/problemset/task/1081/) | 频率统计、枚举倍数          |
| **Count the Cows** | [CSES Counting Numbers](https://cses.fi/problemset/task/2220/) | 基础数位 DP                 |
|                    | [P2602 数字计数](https://www.luogu.com.cn/problem/P2602)     | 数位统计、大范围计数        |

```c++
//Problem2-Modern Art 3
#include<iostream>
#include<cmath>
using namespace std;
const int Nmax=305;
int a[Nmax];
int dp[Nmax][Nmax];
int main(){
    int n;
    cin>>n;
    //initialize
    for(int i=0;i<n;i++){
        cin>>a[i];
        dp[i][i]=1;
    }
    //Interval DP
    for(int i=2;i<=n;i++){
        for(int l=0;l+i-1<n;l++){
            int r=l+i-1;
            //case 1
            dp[l][r]=1+dp[l+1][r];
            //case 2 
            for(int k=l+1;k<=r;k++){
                if(a[l]==a[k]){
                //1
                if(l+1==k){
                    dp[l][r]=min(dp[l][r],dp[k][r]);
                }//2
                else{
                    dp[l][r]=min(dp[l][r],dp[l+1][k-1]+dp[k][r]);
                }
                }
            }
        }
    }
    cout<<dp[0][n-1]<<"\n";
    return 0;
}
```

***

## USACO 2021 February Contest, Silver

| Silver 题目           | 洛谷前置题                                                   | 主要训练内容                             |
| --------------------- | ------------------------------------------------------------ | ---------------------------------------- |
| **Comfortable Cows**  | [P1162 填涂颜色](https://www.luogu.com.cn/problem/P1162)     | 二维网格、四方向检查、Flood Fill         |
|                       | [P2895 流星雨](https://www.luogu.com.cn/problem/P2895)       | 队列、动态加入新状态、避免重复处理       |
| **Year of the Cow**   | [P1209 修理牛棚](https://www.luogu.com.cn/problem/P1209)     | 排序、计算相邻空隙、贪心选择最大的空隙   |
| **Just Green Enough** | [P1719 最大加权矩形](https://www.luogu.com.cn/problem/P1719) | 枚举上下边界，将二维矩阵压缩成一维       |
|                       | [P1950 长方形](https://www.luogu.com.cn/problem/P1950)       | 统计满足条件的全空矩形，与目标问题最接近 |

```c++
//Problem 1. Comfortable Cows
//1.每输入一头牛，如果这头牛位置之前没有牛，就添加这头牛计数cnt+1。
//2.每有一个新牛加入，就加入队列，并检查自己、上下左右四个点的状态。
//3.取出队首位置，如果上下左右有三个牛，在另一空白的位置加入牛
//4.每次加牛的时候，cnt++,加入队列并重复步骤3，可能会有新的连锁反应。
//5.队列为空时输出cnt-i
#include <bits/stdc++.h>
using namespace std;
int dx[4] = {1, 0, -1, 0};
int dy[4] = {0, 1, 0, -1};
set<pair<int, int>> cows;
queue<pair<int, int>> q;
int cnt = 0;  // 当前棋盘上实际存在的奶牛总数
bool hasCow(int x, int y) {
    return cows.count({x, y}) > 0;
}
// 添加一头新牛
void addCow(int x, int y) {
    // 这个位置已经有牛，不重复添加
    if (hasCow(x, y)) {
        return;
    }
    cows.insert({x, y});
    cnt++;
    // 新牛可能引发连锁反应
    q.push({x, y});
}
// 检查某头奶牛是否舒适
void checkCow(int x, int y) {
    // 空位置不需要检查
    if (!hasCow(x, y)) {
        return;
    }
    int neighborCount = 0;
    int emptyX = 0;
    int emptyY = 0;
    for (int d = 0; d < 4; d++) {
        int nx = x + dx[d];
        int ny = y + dy[d];
        if (hasCow(nx, ny)) {
            neighborCount++;
        } else {
            // 如果最终恰好有三个邻居，
            // 这里保存的就是唯一的空位置
            emptyX = nx;
            emptyY = ny;
        }
    }
    // 恰好有三个相邻奶牛，补上唯一的空位置
    if (neighborCount == 3) {
        addCow(emptyX, emptyY);
    }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int N;
    cin >> N;
    for (int i = 1; i <= N; i++) {
        int x, y;
        cin >> x >> y;
        // 如果这个位置之前已被自动添加，
        // addCow 不会重复添加，cnt 也不会增加
        addCow(x, y);
        // 处理所有连锁反应
        while (!q.empty()) {
            auto [cx, cy] = q.front();
            q.pop();
            // 新牛自己可能是舒适奶牛
            checkCow(cx, cy);
            // 新牛可能让上下左右的奶牛变得舒适
            for (int d = 0; d < 4; d++) {
                int nx = cx + dx[d];
                int ny = cy + dy[d];
                checkCow(nx, ny);
            }
        }
        // 实际奶牛总数 - 输入奶牛数量 = 自动添加数量
        cout << cnt - i << '\n';
    }
    return 0;
}
```

```c++
//Problem 2. Year of the Cow
//1.以12年为一轮，统计哪些层有祖先，每一层距离就是12年。
//2.计算祖先层之间的层数距离，并加上第0层到最低层距离，并从大到小排序。
//3.第一次跳是从0层到最高层的距离
//4.以贪心方式选择前k-1个为时间穿越，剩余的累积12*层数差距
//5.输出累积值。
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int N, K;
    cin >> N >> K;
    vector<ll> layers;
    // 把第0层加入
    layers.push_back(0);
    for (int i = 0; i < N; i++) {
        ll year;
        cin >> year;
        // 向上取整，找到祖先对应的牛年层
        ll layer = (year + 11) / 12;
        layers.push_back(layer);
    }
    // 排序并去重
    sort(layers.begin(), layers.end());
    layers.erase(
        unique(layers.begin(), layers.end()),
        layers.end()
    );
    // 如果不跳过任何空档，需要经过的总时间
    ll answer = layers.back() * 12;
    vector<ll> gaps;
    // 计算相邻有效层之间的空白层数
    for (int i = 1; i < (int)layers.size(); i++) {
        ll emptyLayers = layers[i] - layers[i - 1] - 1;
        // 跳过这个空档能够节省的时间
        gaps.push_back(emptyLayers * 12);
    }
    // 优先选择最大的空档
    sort(gaps.rbegin(), gaps.rend());
    // K次传送最多跳过K-1个空档
    int canSkip = min(K - 1, (int)gaps.size());
    for (int i = 0; i < canSkip; i++) {
        answer -= gaps[i];
    }
    cout << answer << '\n';
    return 0;
}

```

