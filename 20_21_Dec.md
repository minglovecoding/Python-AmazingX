## USACO 2020 December Contest, Gold

1. 多源 BFS 与距离场建模（**2、3必做**）

| 顺序 | 题目                                                         | 训练重点                          |
| ---- | ------------------------------------------------------------ | --------------------------------- |
| 1    | [P1443 马的遍历](https://www.luogu.com.cn/problem/P1443)     | 基础网格 BFS、一次 BFS 求全部距离 |
| 2    | [P1332 血色先锋队](https://www.luogu.com.cn/problem/P1332)   | 标准多源 BFS                      |
| 3    | [P2895 Meteor Shower S](https://www.luogu.com.cn/problem/P2895) | 随时间变化的危险区域、受限 BFS    |
| 4    | [P3132 Angry Cows G](https://www.luogu.com.cn/problem/P3132) | 扩张半径、距离与覆盖范围建模      |
| 5    | [P7151 Replication G](https://www.luogu.com.cn/problem/P7151) | 多源 BFS＋距离场＋分层扩张        |

2. 字符串计数 DP 与有限状态设计（**2必做**）

| 顺序 | 题目                                                         | 训练重点                      |
| ---- | ------------------------------------------------------------ | ----------------------------- |
| 1    | [P1077 摆花](https://www.luogu.com.cn/problem/P1077)         | 基础计数 DP                   |
| 2    | [P2679 子串](https://www.luogu.com.cn/problem/P2679)         | 字符串分段、计数 DP、状态设计 |
| 3    | [P4052 文本生成器](https://www.luogu.com.cn/problem/P4052)   | 字符串自动机＋计数 DP         |
| 4    | [P3193 GT考试](https://www.luogu.com.cn/problem/P3193)       | 有限状态自动机、字符串计数    |
| 5    | [P7152 Bovine Genetics G](https://www.luogu.com.cn/problem/P7152) | 常数状态压缩、字符串分段计数  |

3. 计算几何枚举、双指针与去重计数（**4、5必做**）

| 顺序 | 题目                                                         | 训练重点                       |
| ---- | ------------------------------------------------------------ | ------------------------------ |
| 1    | [P1102 A-B 数对](https://www.luogu.com.cn/problem/P1102)     | 排序后计数                     |
| 2    | [P1638 逛画展](https://www.luogu.com.cn/problem/P1638)       | 双指针、集合随窗口变化         |
| 3    | [P2280 激光炸弹](https://www.luogu.com.cn/problem/P2280)     | 二维区域与固定窗口             |
| 4    | [P7149 Rectangular Pasture S](https://www.luogu.com.cn/problem/P7149) | 枚举边界、包围盒、二维集合计数 |
| 5    | [P7153 Square Pasture G](https://www.luogu.com.cn/problem/P7153) | 几何分类＋双指针＋去重         |

***

### USACO 2020 December Contest, Gold

### Problem 1. Replication

``` c++
//1.实时模拟这个过程太复杂，不可取。结合题目多个起点，可采用多源BFS。
//2.曼哈顿菱形的性质：｜xn-x|+｜yn-y｜<= r,因此从菱形半径一定小于该点到周围最近石头的距离
//3.求所有可能到达的空格，可由所有可到达中心扩张而成。time / D < rockDist[x][y]，半径为多大最终受限于到最近石头距离。
//4.求空格到石头的最短距离可以反推成多源BFS从石头到空格的位置。
//5.对所有可达中心枚举扩张时间复杂度过高，所以优化用最大能力优先的多源BFS，从中心散发能量，中心点记录的是最大安全能量rockDist[x][y] - 1（考虑来回踱步可能会增加半径），每个grid记录最大的能量值。
//6.最后统计有多少个空格有能量（包含0，0没有扩张能力，但到过该点）。
#include<bits/stdc++.h>
using namespace std;
const int Nmax=1005;
int N,D;
int dx[4]={1,0,-1,0};
int dy[4]={0,1,0,-1};
int rockDist[Nmax][Nmax];
int firstArrive[Nmax][Nmax];
int maxPower[Nmax][Nmax];
queue<pair<int,int>>q1;
queue<pair<int,int>>q2;
priority_queue<tuple<int,int,int>>pq3;
bool border(int x,int y){
    return x>=0&&x<N&&y>=0&&y<N;
}
int main(){
    string grid[Nmax];
    cin>>N>>D;
    for(int i=0;i<N;i++){
        cin>>grid[i];
    }
    //Stage 1
    memset(rockDist,-1,sizeof(rockDist));
    for(int i=0;i<N;i++){
        for(int j=0;j<N;j++){
            if(grid[i][j]=='#'){
                rockDist[i][j]=0;
                q1.push({i,j});
            }
        }
    }
    while(!q1.empty()){
        auto [x,y]=q1.front();
        q1.pop();
        for(int i=0;i<4;i++){
            int nx=x+dx[i];
            int ny=y+dy[i];
            if(!border(nx,ny)) continue;
            if(rockDist[nx][ny]!=-1) continue;
            rockDist[nx][ny]=rockDist[x][y]+1;
            q1.push({nx,ny});
        }
    }
    //Stage 2
    memset(firstArrive,-1,sizeof(firstArrive));
    for(int i=0;i<N;i++){
        for(int j=0;j<N;j++){
            if(grid[i][j]=='S'){
                firstArrive[i][j]=0;
                q2.push({i,j});
            }
        }
    }
    while(!q2.empty()){
        auto [x,y]=q2.front();
        q2.pop();
        //power
        int time=firstArrive[x][y];
        if(time/D>rockDist[x][y]-1) continue;
        q3.push({rockDist[x][y]-1,x,y});
        for(int i=0;i<4;i++){
            int nx=x+dx[i];
            int ny=y+dy[i];
            if(!border(nx,ny)) continue;
            if(grid[nx][ny]=='#') continue;
            if(firstArrive[nx][ny]!=-1) continue;
            firstArrive[nx][ny]=firstArrive[x][y]+1;
            q2.push({nx,ny});
        }
    }
    //stage3
    memset(maxPower,-1,sizeof(maxPower));
    while(!pq3.empty()){
        auto [power,x,y] = pq3.top();
        pq3.pop();
        if(maxPower[x][y]>=power) continue;
        maxPower[x][y]=power;
        if(power==0) continue;
        for(int i=0;i<4;i++){
            int nx=x+dx[i];
            int ny=y+dy[i];
            if(!border(nx,ny)) continue;
            if(grid[nx][ny]=='#') continue;
            int nextPower=power-1;
            if(maxPower[nx][ny]<nextPower){
                pq3.push({nextPower,nx,ny});
            }
        }
    }
    //Output
    int ans=0;
    for(int i=0;i<N;i++){
        for(int j=0;j<N;j++){
            if(maxPower[i][j]!=-1) ans++;
        }
    }
    cout<<ans<<endl;
    return 0;
}
```

### Problem3-Square Pasture G

### 思路：

1. 将所有奶牛按照 `x` 坐标排序。

2. 枚举 `l、r`，作为子集中最左和最右的奶牛，宽度为：

   $$
   W=x_r-x_l
   $$

3. 取出 `l～r` 之间的所有奶牛，按照 `y` 坐标排序，得到数组 `ys`。

4. 找到奶牛 `l、r` 在 `ys` 中的位置：

   ```cpp
   p = min(posL, posR);
   q = max(posL, posR);
   ```

5. 枚举所选区间的最低位置 `a`。因为区间必须包含 `l、r`，所以：

   $$
   a\le p\le q\le b
   $$

6. 为了保证高度不超过宽度，需要满足：

   $$
   ys[b]-ys[a]\le W
   $$

   使用 `upper_bound` 找到最大的合法位置 `maxB`：

   ```cpp
   int maxB =
       upper_bound(ys.begin(), ys.end(), ys[a] + W)
       - ys.begin() - 1;
   ```

   右端点 `b` 可以选择 `q～maxB`，合法数量为：

   ```cpp
   maxB - q + 1
   ```

7. 上述过程统计的是 W>= H。交换所有奶牛的 `x、y` 后再计算一次，统计 H<=H。

8. 当 W=H 时会被计算两次，需要减去一次，最后加上空集：

   ```cpp
   answer = count(W >= H)
          + count(H >= W)
          - count(W == H)
          + 1;
   ```

### 时间复杂度

$$
O(N^3\log N)
$$

```c++
#include <bits/stdc++.h>
using namespace std;
using PII = pair<int, int>;
int N;
int ans;
int equalSquare;
vector<PII> cows;
//统计最小包围矩形满足 width >= height 的子集
void solve() {
    sort(cows.begin(), cows.end());
    //L、R 是子集中最左、最右的奶牛
    for (int L = 0; L < N; L++) {
        for (int R = L + 1; R < N; R++) {
            int width = cows[R].first - cows[L].first;
            int yL = cows[L].second;
            int yR = cows[R].second;
            //左右端点的纵向距离不能超过正方形边长
            if (abs(yL - yR) > width) continue;
            //收集L和R中间奶牛的y坐标
            vector<int> ys;

            for (int i = L + 1; i < R; i++) {
                ys.push_back(cows[i].second);
            }
            sort(ys.begin(), ys.end());
            /*
             * 正方形下边界 bottom 必须满足：
             *
             * bottom <= min(yL,yR)
             * bottom + width >= max(yL,yR)
             */
            int low = max(yL, yR) - width;
            int high = min(yL, yR);
            /*
             * 正方形上下移动时，只有两种位置会改变集合：
             * 1. bottom = 某头奶牛的y+1 离开
             * 2. bottom = 某头奶牛的y - width 进入
             * 因此只枚举这些关键位置。
             */
            vector<int> candidateBottom;
            candidateBottom.push_back(low);
            for (int y : ys) {
                candidateBottom.push_back(y+1);
                candidateBottom.push_back(y - width);
            }
            sort(candidateBottom.begin(), candidateBottom.end());
            candidateBottom.erase(
                unique(candidateBottom.begin(),
                       candidateBottom.end()),
                candidateBottom.end()
            );
            /*
             * 防止不同bottom位置产生相同集合。
             * 用区间 [first,last] 表示当前包含的内部奶牛。
             */
            pair<int, int> previousState = {-2, -2};
            for (int bottom : candidateBottom) {
                if (bottom < low || bottom > high) continue;
                int top = bottom + width;
                //第一个y >= bottom的位置
                int first =
                    lower_bound(ys.begin(), ys.end(), bottom)
                    - ys.begin();
                //第一个y > top的位置
                int last =
                    upper_bound(ys.begin(), ys.end(), top)
                    - ys.begin() - 1;
                pair<int, int> currentState;
                if (first > last) {
                    //没有包含任何内部奶牛
                    currentState = {-1, -1};
                } else {
                    currentState = {first, last};
                }
                //相同集合不重复计算
                if (currentState == previousState) continue;
                previousState = currentState;
                ans++;
                /*
                 * 求当前子集最小包围矩形的高度，
                 * 判断是否 width == height。
                 */
                int minY = min(yL, yR);
                int maxY = max(yL, yR);
                if (first <= last) {
                    minY = min(minY, ys[first]);
                    maxY = max(maxY, ys[last]);
                }
                if (maxY - minY == width) {
                    equalSquare++;
                }
            }
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> N;
    cows.resize(N);
    for (auto &[x, y] : cows) {
        cin >> x >> y;
    }
    //空集和只包含一头奶牛的子集
    ans = N + 1;
    //统计 width >= height
    solve();
    //交换坐标，统计原来的 height >= width
    for (auto &[x, y] : cows) {
        swap(x, y);
    }
    solve();
    //width == height 的子集被统计两次
    ans -= equalSquare / 2;
    cout << ans << '\n';
    return 0;
}
```

### USACO 2020 December Contest, Silver

### Problem 2. Rectangular Pasture

```c++
//1.把x、y坐标离散化为1到N
//2.先对y轴枚举区间（1-1、1-2...1-N;2-2、2-3、2-N;...N-N）
//3.记录每个区间最上方和最下方的元素的x轴x1、x2（且x1<x2），并计算x1本身及左边元素的个数num1，和x2本身及右边元素num2，num1乘以num2即为该区间所有子集个数。
//4.用前缀和方式优化找num1和num2的时间
//5.累加不同区间的子集个数，最后再加空集即为所有子集个数。
#include<bits/stdc++.h>
using namespace std;
const int Nmax=2505;
int cow[Nmax][Nmax];
vector<pair<int,int>>cowMap;
int areaCow(int x1,int y1,int x2,int y2){
    return cow[x2+1][y2+1]-cow[x2+1][y1]-cow[x1][y2+1]+cow[x1][y1];
}
int main(){
    int N;
    int x,y;
    cin>>N;
    for(int i=0;i<N;i++){
        cin>>x>>y;
        cowMap.push_back(make_pair(x,y));
    }
    //1.把x、y坐标离散化为1到N
    sort(cowMap.begin(),cowMap.end());
    for(int i=0;i<N;i++){
        cowMap[i].first=i+1;
    }
    sort(cowMap.begin(),cowMap.end(),[](const pair<int,int>&a,const pair<int,int>&b){return a.second<b.second;});
    for(int i=0;i<N;i++){
        cowMap[i].second=i+1;
    }
    //4.用前缀和方式优化找num1和num2的时间
    for(auto mapp:cowMap){
        cow[mapp.first][mapp.second]=1;
    }
    for(int i=1;i<=N;i++){
        for(int j=1;j<=N;j++){
            cow[i][j]+=cow[i-1][j]+cow[i][j-1]-cow[i-1][j-1];
        }
    }
    //3.记录每个区间最上方和最下方的元素的x轴x1、x2（且x1<x2）
    //并计算x1本身及左边元素的个数num1，和x2本身及右边元素num2，num1乘以num2即为该区间所有子集个数。
    long long ans=0;
    for(int i=0;i<N;i++){
        for(int j=i;j<N;j++){
            int x1=min(cowMap[i].first,cowMap[j].first)-1;
            int x2=max(cowMap[i].first,cowMap[j].first)-1;
            int num1=areaCow(0,i,x1,j);
            int num2=areaCow(x2,i,N-1,j);
            ans+=1LL*num1*num2;
        }
    }
    //5.累加不同区间的子集个数，最后再加空集即为所有子集个数。
    cout<<ans+1<<endl;
    return 0;
}
```

