## USACO 2021 January Contest, Gold

| Gold题目                      | 洛谷必做题                                                   | 主要训练内容                              |
| ----------------------------- | ------------------------------------------------------------ | ----------------------------------------- |
| 1. P7296 Uddered but not Herd | [P1433 吃奶酪](https://www.luogu.com.cn/problem/P1433)       | 状态压缩 DP 入门：集合状态、枚举新增元素  |
|                               | [P3694 邦邦的大合唱站队](https://www.luogu.com.cn/problem/P3694?utm_source=chatgpt.com) | 按类别排列的状态压缩 DP，与原题建模最接近 |
| 2. P7297 Telephone            | [P4554 小明的游戏](https://www.luogu.com.cn/problem/P4554?utm_source=chatgpt.com) | 0-1 BFS：边权只有 0 和 1                  |
|                               | [P4568 飞行路线](https://www.luogu.com.cn/problem/P4568?utm_source=chatgpt.com) | 分层图与扩展状态最短路                    |
| 3. P7298 Dance Mooves         | [P7299 Dance Mooves（Silver）](https://www.luogu.com.cn/problem/P7299?utm_source=chatgpt.com) | 置换环                                    |
|                               | [P1638 逛画展](https://www.luogu.com.cn/problem/P1638?utm_source=chatgpt.com) | 双指针、滑动窗口、计数数组                |

***

### Problem 2. Telephone

```c++
//1≤N≤5⋅10^4
//从奶牛1传到奶牛N的时间

//case1 显示图建模
//1.首先算一个NxN的dist图表，图表内的值表示第i头牛到第j头牛的代价，方法是首先看i和j牛的品种是什么，再查表看这两头牛是否愿意交换信息，如果愿意，那就能算出代价=|i-j|，如果不愿意，那就输出INF。
//2.用Dijkstra算法算从1到N的最短时间。

//full solution 隐式图建模
//1.求最短路径通常是Dijstra和BFS，但BFS通常是一步一步处理，于是我们观察|i-j|可以拆成从位置i到位置j的一格一格移动
//2.建立状态{pos,type}表示当前携带信息的奶牛品种是type现在到达pos
//3.dist[pos][type]表示到达{pos,type}最小累积费用
//4.状态转移有三种可能：1.(pos,type) -> (pos-1,type) dist[pos-1][type]=dist[pos][type]+1;
//                   2.(pos,type) -> (pos+1,type) dist[pos+1][type]=dist[pos][type]+1;
//                   3.(pos,type) -> (pos,breed[pos]) dist[pos][breed[pos]]=dist[pos][type];
//5.使用0-1 BFS,转换时的转态放到deque队头,位置变化时的状态加入deque尾部
//6.初始化状态(1,breed[1]) 放入队列，dist[1][breed[1]]=0;
//7.输出dist[N][breed[N]] 如果答案是INF，输出-1
#include<bits/stdc++.h>
using namespace std;
const int Nmax=5e4+5;
const int Kmax=55;
const int INF=0x3f3f3f3f;
int N,K;
int breed[Nmax];
char Kmap[Kmax][Kmax];
int dist[Nmax][Kmax];
deque<pair<int,int>>dq;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin>>N>>K;
    memset(dist,0x3f,sizeof(dist));
    for(int i=1;i<=N;i++){
        cin>>breed[i];
    }
    for(int i=1;i<=K;i++){
        for(int j=1;j<=K;j++){
            cin>>Kmap[i][j];
        }
    }

    dist[1][breed[1]]=0;
    dq.push_front({1,breed[1]});

    while(!dq.empty()){
        auto [pos,type]=dq.front();
        dq.pop_front();
        int currentDist=dist[pos][type];
        //1.传递给当前牛
        int nextType=breed[pos];
        if(Kmap[type][nextType]=='1'&&dist[pos][nextType]>currentDist){
            dist[pos][nextType]=currentDist;
            dq.push_front({pos,nextType});
        } 
        //2.向右传递
        if(pos>1&&dist[pos-1][type]>currentDist+1){
            dist[pos-1][type]=currentDist+1;
            dq.push_back({pos-1,type});
        }
        //3.向左传递
        if(pos<N&&dist[pos+1][type]>currentDist+1){
            dist[pos+1][type]=currentDist+1;
            dq.push_back({pos+1,type});
        }
    }

    if(dist[N][breed[N]]==INF) cout<<-1<<endl;
    else cout<<dist[N][breed[N]]<<endl;
    return 0;
}
```

### Problem 1. Uddered but not Herd

> 这是一道选择字母排序，使听到的字符串重唱字母歌次数最小的问题。

每个字母的位置为 `position[c]`。对于字符串中相邻的两个字符 a -> b:

- `position[a] < position[b]`：可以在同一遍字母歌中听到；

- `position[a] > position[b]`：`b` 已经唱过去了，必须开始下一遍；

- `a == b`：一遍只唱一次这个字母，也必须开始下一遍。

字符串长度为10^5，每个测试例子不超过20个，因此可以对出现的字母进行状压。

```c++
1.统计相邻关系，将出现的字母离散化，统计字符串中 a -> b 出现的次数。
2.dp[mask]表示已经安排 mask 中的字母时，最少产生的换轮次数。
3.状态转移枚举未安排的字母 j，将它放到当前排列最后。
4.最后加上第一遍，以及相同字母造成的换轮。
#include<bits/stdc++.h>
using namespace std;
const int INF=0x3f3f3f3f;
const int Kmax=20;  //记录所有出现的字母，个数K一定小于或等于20
int cnt[Kmax][Kmax];  //记录Nhoj听到字符里相邻字符出现的链接次数
vector<int>id(26,-1); //记录出现字符的id
int addCost[Kmax][1<<Kmax];
int dp[1<<Kmax];
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    string s;
    cin>>s;
    //c -> index
    int K=0;
    for(auto c:s){
        int index=c-'a';
        if(id[index]==-1){
            id[index]=K++;
        }
    }
    //统计cnt[i][j] 
    for(int i=0;i<s.size()-1;i++){
        int a=id[s[i]-'a'];
        int b=id[s[i+1]-'a'];
        cnt[a][b]++;
    }
    int totalState=1<<K;  //K个字符的排列状态
    memset(addCost,0,sizeof(addCost));
    //计算j放在mask后的新代价
    for(int j=0;j<K;j++){
        for(int mask=1;mask<totalState;mask++){
            //找到mask最低的位1
            int lowBit=__builtin_ctz(mask);
            //找到去除这个位的mask
            int previousMask=mask^(1<<lowBit);
            addCost[j][mask]=addCost[j][previousMask]+cnt[j][lowBit];
        }
    }
    memset(dp,0x3f,sizeof(dp));
    dp[0]=0;
    for(int mask=0;mask<totalState;mask++){
        for(int j=0;j<K;j++){
            if(mask&(1<<j)) continue;
            int newMask=mask|(1<<j);
            dp[newMask]=min(dp[newMask],dp[mask]+addCost[j][mask]);
        }
    }
    //+第一遍
    int res=1+dp[totalState-1];
    //相同字符
    for(int i=0;i<K;i++){
        res+=cnt[i][i];
    }
    cout<<res<<'\n';
    return 0;
}
```
