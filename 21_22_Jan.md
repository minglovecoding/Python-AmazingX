### USACO 2022 January Contest, Gold

### Problem 2. Farm Updates

**题意：**N个农场执行Q个操作，每个操作有三种可能：Deactive x、建边、删边。一个农场在自己是active或能连接到active的农场时算是relevant，农场在最开始都是relevant，问每个农场执行操作后最后一次是relevant的时间。

**思路：**这个题本质上在问不同操作阶段后，节点所在的连通区域是否有active节点，所以这很显然是**连通性问题**。由于正序操作会改变连通块状态，很难维护，巧妙的用倒序操作不断加边反而容易多，因此此题很显然要用到**Offline Reverse Processing**。在加边或加点的过程中，如果连通区域的某一点被更新成relevant，那么整个连通区域都需要更新状态，因此最好用到**DFS**进行连通块染色。 

**注意点**：no two updates of type R have the same value of e. 在倒序过程中，一个 farm 一旦被标记为 relevant，就不会再变回 irrelevant。

> 因此此题考察的是**离线倒序处理+图连通性+DFS**问题。

  首先对于倒序处理，正序减边对应倒序加边，此时如果u、v任意一点relevant，另一点的整个连通块都会变成relevant。正序Deactive对应倒序active，同样也会使整个连通块relevant。但是对于正序加边倒序并不需要做任何动作。因为题意说Add a road between two active farms x and y，也就是说加入这个边时两边都是relevant，那减边并不会影响两点的状态。

  因此整个解法的逻辑可以遵循：

```c++
第一步
读取所有 Q 次操作
        ↓
确定最终时刻还存在的道路
        ↓
确定最终仍然 active 的 farm
        ↓
从这些 active farms DFS连通区域
        ↓
这些active节点答案 = Q

第二步
倒序 Q ... 1

遇到 D x
    ↓
相当于 x 重新 active
    ↓
DFS(x)

遇到 R e
    ↓
相当于把 edge e 加回来
    ↓
如果连接到了 relevant component
    ↓
DFS 新 component

遇到 A
    ↓
ignore

//时间复杂度O(N+Q)
```

```c++
#include <bits/stdc++.h>
using namespace std;
struct Operation {
    char type;
    int x, y;   // D: x=farm
                // A: x=u, y=v
                // R: x=edge_id
};
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
  
    int N, Q;
    cin >> N >> Q;
    vector<Operation> ops(Q + 1); //离线处理需要存所有操作
    vector<pair<int, int>> edges(1); //使后续加入的边使从1开始
    vector<bool> removed(1); //删除的边也是从1开始
    vector<bool> active(N + 1, true);
    int edgeCount = 0;
    //1.读操作
    for (int i = 1; i <= Q; i++) {
        char type;
        cin >> type;
        if (type == 'D') {
            int x;
            cin >> x;
            ops[i] = {type, x, 0};
            // 最终状态下 x 已经 inactive
            active[x] = false;
        }
        else if (type == 'A') {
            int u, v;
            cin >> u >> v;
            edgeCount++; //统计是第几条边
            edges.push_back({u, v});
            removed.push_back(false);
            // 保存这次 A 对应的是哪条边
            ops[i] = {type, edgeCount, 0};
        }
        else if (type == 'R') {
            int edgeId;
            cin >> edgeId;
            ops[i] = {type, edgeId, 0};
            // 这条边在最终状态不存在
            removed[edgeId] = true;
        }
    }
    // 2. 建立Final的图
    vector<vector<int>> adj(N + 1); //领接表
    for (int id = 1; id <= edgeCount; id++) {
        if (!removed[id]) {
            auto [u, v] = edges[id];
            adj[u].push_back(v);
            adj[v].push_back(u);
        }
    }
    // ans[u] = farm u 最后 relevant 的时间
    vector<int> ans(N + 1, -1);
    // relevant[u] = 在当前倒序状态中，u 是否已经 relevant
    vector<bool> relevant(N + 1, false);

    // 用flood函数将整个连通块标记为 relevant
    auto flood = [&](int start, int time) {
        stack<int> st;
        st.push(start);
        while (!st.empty()) {
            int u = st.top();
            st.pop();
            if (relevant[u])
                continue;
            relevant[u] = true;
            ans[u] = time;
            for (int v : adj[u]) {
                if (!relevant[v]) {
                    st.push(v);
                }
            }
        }
    };
    // 3. 最终时刻：
    //    所有仍 active 的 farm 所在连通块都 relevant
    for (int u = 1; u <= N; u++) {
        if (active[u] && !relevant[u]) {
            flood(u, Q);
        }
    }
    // 4. 倒序处理操作
    for (int i = Q; i >= 1; i--) {
        char type = ops[i].type;
        if (type == 'D') {
            int x = ops[i].x;
            if (!relevant[x]) {
                flood(x, i - 1);// 倒序：x 重新 activate，回到上一步time-1;
            }
        }
        else if (type == 'R') {
            int edgeId = ops[i].x;
            auto [u, v] = edges[edgeId];// 倒序：把边加回来
            adj[u].push_back(v);
            adj[v].push_back(u);
            // 如果一边已经 relevant，另一边整个 Graph component 都会变 relevant
            if (relevant[u] && !relevant[v]) {
                flood(v, i - 1);
            }
            else if (relevant[v] && !relevant[u]) {
                flood(u, i - 1);
            }
        }
        else if (type == 'A') {
            // do nothing
        }
    }
    // 5. 输出答案
    for (int u = 1; u <= N; u++) {
        cout << ans[u] << '\n';
    }
    return 0;
}
```

