## Python Programming Beginner Course

## 📚 What Can Python Do?

- USACO, CCC competitions
- Web scraping & data analysis
- Automation scripts & embedded development
- Web development & game development
- Artificial Intelligence & Machine Learning (image recognition, NLP, prediction models)

## 🖥️ Essential Software

- VS Code (for writing code)
- GitHub (open-source code hosting)
- ChatGPT (for checking & understanding code)

## 🧑🏽‍💻 Coding Practice Websites

- [LeetCode](https://leetcode.cn/)
- [USACO](https://usaco.org/index.php)
- Codeforces (CF)

## 🗓️ Course Schedule & Learning Goals

| Lesson | Topic                              | Goals                                                        |
| ------ | ---------------------------------- | ------------------------------------------------------------ |
| 1      | Introduction + Python syntax       | Understand programming & Python basics, install environment & run first program |
| 2      | Variables & Data Types             | Learn about integers, floats, strings, booleans, etc.        |
| 3      | Input & Output                     | Use `input()` and `print()` for basic interaction            |
| 4      | Operators & Expressions            | Perform operations (add/sub/multiply/divide, modulus, compare) |
| 5      | Conditional Statements (if...else) | Construct logic with conditions                              |
| 6      | Loops: for / while                 | Use loops to handle repeated tasks                           |
| 7      | Lists & String Manipulation        | Master common data structures and methods                    |
| 8      | Defining & Calling Functions       | Learn function encapsulation and code organization           |
| 9      | Project: Number Guessing Game      | Apply all learned concepts                                   |
| 10     | Summary & Expansion                | Showcase projects and explore creative coding                |

## 📝 Sample Code

### 📌 code 1: First Python Program

```python
print("Hello world!")
```

### 📌 code 2: Input & output

```python
name = input("Please enter your name: ")
print("Hello, " + name + "!")
```

## Python Data Types

### **Numbers**

Four numeric types: integer, boolean, float, and complex.

- int (e.g., 1)
- bool (True/False)
- float (e.g., 1.23)
- complex (e.g., 1 + 2j)

### **String**

- Single `'` and double `"` quotes are the same
- Triple quotes allow multi-line strings
- Backslash `\` escapes characters
- Prefix `r` to prevent escapes
- Use slicing: `str[start:end:step]`

```python
word = 'string'
sentence = "This is a sentence."
paragraph = """This is a paragraph,
spanning multiple lines."""
```

### 📌 code 3: String slicing

```python
str='123456789'
 
print(str)                 
print(str[0])              
print(str[2:5])            
print(str[2:])             
print(str[1:5:2])          
print(str[0:-1])           
print(str * 2)             
print(str + 'hello')       
print('------------------------------')
 
print('hello world\n\n')   
print(r'hello world')      
```

### 📌 code 4: Check if number is positive

```c++
num = int(input("Enter an integer: ")) 
if num > 0:
    print("This is a positive number")
elif num < 0:
    print("This is a negative number")
else:
    print("This is zero")
```

### 📌 code 5: For loop

```python
for i in range(3):
    print("hello world")
```

### 📌 code 6: Number Guessing Game

```python
import random
answer = random.randint(1, 100)
guess = -1
while guess != answer:
    guess = int(input("Guess a number between 1 and 100: "))
    if guess < answer:
        print("Too low")
    elif guess > answer:
        print("Too high")
    else:
        print("Congratulations! You guessed it!")
```

### print() without newline

```python
x="a"
y="b"

print(x)
print(y)

print('---------')

print(x, end=" ")
print(y, end=" ")
print()
```

### Python Variables

```c++
counter = 100
miles = 1000.0
name = "John"
a = b = c = 1
a, b, c = 1, 2, "john"
 
print(counter)
print(miles)
print(name)
```

##### Standard Data Types

- Numbers
- Strings
- Lists
- Tuples
- Dictionaries

##### Strings

```python
str = 'Hello World!'
print(str)
print(str[0])
print(str[2:5])
print(str[2:])
print(str * 2)
print(str + "TEST")
```

##### Lists

```python
list = ['runoob', 786, 2.23, 'john', 70.2]
tinylist = [123, 'john']

print(list)
print(list[0])
print(list[1:3])
print(list[1:4:2])
print(list[2:])
print(tinylist * 2)
print(list + tinylist)
```

##### Tuples

```python
tuple = ('runoob', 786, 2.23, 'john', 70.2)
tinytuple = (123, 'john')

print(tuple)
print(tuple[0])
print(tuple[1:3])
print(tuple[2:])
print(tinytuple * 2)
print(tuple + tinytuple)
```

##### Dictionaries

```python
dict = {}
dict['one'] = "This is one"
dict[2] = "This is two"
 
tinydict = {'name': 'runoob','code':6734, 'dept': 'sales'}
 
print(dict['one'])
print(dict[2])
print(tinydict)
print(tinydict.keys())
print(tinydict.values())
print(tinydict['name'])
```

| Function                 | Description                                                  |
| ------------------------ | ------------------------------------------------------------ |
| `int(x [, base])`        | Converts *x* to an integer.                                  |
| `long(x [, base])`       | Converts *x* to a long integer. *(Note: `long` existed in Python 2; in Python 3 all integers are of type `int`.)* |
| `float(x)`               | Converts *x* to a floating-point number.                     |
| `complex(real [, imag])` | Creates a complex number.                                    |
| `str(x)`                 | Converts object *x* to a string.                             |
| `repr(x)`                | Converts object *x* to an expression string (developer-friendly representation). |
| `eval(str)`              | Evaluates a valid Python expression in a string and returns the result. |
| `tuple(s)`               | Converts a sequence *s* into a tuple.                        |
| `list(s)`                | Converts a sequence *s* into a list.                         |
| `set(s)`                 | Converts *s* to a mutable set.                               |
| `dict(d)`                | Creates a dictionary. *d* must be a sequence of *(key, value)* tuples. |
| `frozenset(s)`           | Converts *s* to an immutable set.                            |
| `chr(x)`                 | Converts an integer *x* to a character.                      |
| `unichr(x)`              | Converts an integer *x* to a Unicode character. *(Python 2 only; in Python 3 use `chr`.)* |
| `ord(x)`                 | Converts a character to its integer Unicode code point.      |
| `hex(x)`                 | Converts an integer to a hexadecimal string.                 |
| `oct(x)`                 | Converts an integer to an octal string.                      |

## Python Operators

### **Python Arithmetic Operators**

| Operator | Description                                                  | Example                                          |
| -------- | ------------------------------------------------------------ | ------------------------------------------------ |
| `+`      | Addition — adds two objects                                  | `a + b` results in **30**                        |
| `-`      | Subtraction — returns the difference or a negative value     | `a - b` results in **-10**                       |
| `*`      | Multiplication — multiplies two numbers or repeats a string  | `a * b` results in **200**                       |
| `/`      | Division — divides x by y                                    | `b / a` results in **2**                         |
| `%`      | Modulus — returns the remainder of division                  | `b % a` results in **0**                         |
| `**`     | Exponent — returns x raised to the power of y                | `a ** b` equals 10²⁰ → **100000000000000000000** |
| `//`     | Floor division — returns the integer part of the quotient (**rounds down**) | `9 // 2` → **4** ; `-9 // 2` → **-5**            |

```python
a = 21
b = 10
c = 0
 
c = a + b
print ("1 - c 的值为：", c)
 
c = a - b
print ("2 - c 的值为：", c)
 
c = a * b
print "3 - c 的值为：", c 
 
c = a / b
print "4 - c 的值为：", c 
 
c = a % b
print "5 - c 的值为：", c
 
# 修改变量 a 、b 、c
a = 2
b = 3
c = a**b 
print "6 - c 的值为：", c
 
a = 10
b = 5
c = a//b 
print "7 - c 的值为：", c
```



------

### Conditional Statements

```python
num = 5     
if num == 3:
    print("num is 3")        
elif num == 2:
    print("num is 2") 
elif num == 1:
    print("num is 1") 
elif num < 0:
    print("num < 0")
else:
    print("roadman")
```

### While Loops

```python
count = 0
while count < 9:
   print("The count is:", count)
   count+=1
print("Good bye!")
```

### For Loops

```python
fruits = ['banana', 'apple', 'mango']
for fruit in fruits:
   print("Current fruit: " + fruit)
print("Good bye!")
```

### Nested Loops

```python
i = 2
while(i < 100):
   j = 2
   while(j <= (i/j)):
      if not(i%j): break
      j = j + 1
   if (j > i/j): print(i, "is a prime number")
   i = i + 1
 
print("Good bye!")
```