## 📌曼哈顿菱形

曼哈顿菱形是指：从一个中心点出发，只能上下左右移动不超过 `r` 步，所有能够到达的格子组成的区域。

曼哈顿距离公式：

$$
d = |x_1-x_2| + |y_1-y_2|
$$

以 `(x, y)` 为中心、半径为 `r` 的曼哈顿菱形，包含所有满足以下条件的格子：

$$
|x_n-x| + |y_n-y| \le r
$$

例如半径 `r = 2`：

```text
    X
  X X X
X X C X X
  X X X
    X
```

其中 `C` 是中心。

在 **USACO Replication** 中，机器人复制 `k` 次后，会形成一个半径为 `k` 的曼哈顿菱形。

***

## 📌滚动数组

### 1. 什么是滚动数组？

滚动数组是动态规划中的一种**空间优化方法**。

如果计算第 i 层时只会用到第 i-1 层，那么更早的状态就不需要继续保存。

例如：

```cpp
dp[i][j] = dp[i - 1][j] + ...
```

原本需要保存全部 n 层：

```
dp[n + 1][m + 1]
```

现在只需要保存两层：

```
dp[2][m + 1]
```

这两层轮流表示“当前层”和“上一层”，因此称为**滚动数组**。

### 2. 当前层和上一层

```c++
int now = i & 1;
int pre = now ^ 1;
```

| i    | `now` | `pre` |
| ---- | ----- | ----- |
| 1    | 1     | 0     |
| 2    | 0     | 1     |
| 3    | 1     | 0     |
| 4    | 0     | 1     |

------

### 3. P2679为什么可以使用滚动数组？

原始状态为：

```c++
f[i][j][p]
g[i][j][p]
```

转移方程为：

```c++
g[i][j][p] =
    g[i - 1][j - 1][p]
    + f[i - 1][j - 1][p - 1];

f[i][j][p] =
    f[i - 1][j][p]
    + g[i][j][p];
```

计算第 i 层时，只需要：

- 上一层 i-1；
- 正在计算的当前层 i。

不需要 i-2,i-3 等更早的状态，所以可以把第一维压缩为 `2`：

```c++
int f[2][205][205];
int g[2][205][205];
```

转移改成：

```c++
g[now][j][p] =
    g[pre][j - 1][p]
    + f[pre][j - 1][p - 1];

f[now][j][p] =
    f[pre][j][p]
    + g[now][j][p];
```

对应关系如下：

| 原状态     | 滚动数组 |
| ---------- | -------- |
| `f[i]`     | `f[now]` |
| `g[i]`     | `g[now]` |
| `f[i - 1]` | `f[pre]` |
| `g[i - 1]` | `g[pre]` |

### 4. 为什么每层都要清空？

`f[now]` 和 `g[now]` 中保存着更早一层的数据。

例如计算第 3 层时：

```c++
now = 1;
pre = 0;
```

此时 `f[1]` 原来保存的是第 1 层。现在需要用它保存第 3 层，所以必须先清空：

```c++
memset(f[now], 0, sizeof(f[now]));
memset(g[now], 0, sizeof(g[now]));
```

否则旧数据可能污染当前结果。

### 5. 优化效果

优化前的空间复杂度：O(nmk)

优化后的空间复杂度：O(mk)

时间复杂度不变，仍然是：O(nmk)

### 6. 总结

当状态满足：

```
dp[i][...] 只依赖 dp[i - 1][...]
```

就可以考虑使用两层滚动数组：

```
dp[2][...]
```

通过：

```
int now = i & 1;
int pre = now ^ 1;
```

让编号 `0` 和 `1` 轮流表示当前层和上一层。

滚动数组只优化空间，不会改变原来的状态定义和状态转移。

***

## 📌置换环

置换表示每个元素经过一整轮操作后会移动到哪里，并且每个位置恰好对应一个元素。

例如：

```
1 → 3
3 → 4
4 → 2
2 → 1
```

从 `1` 开始不断移动：

```
1 → 3 → 4 → 2 → 1
```

最终回到起点，形成一个**置换环**：

```
(1,3,4,2)
```

置换具有一一对应关系，所以所有元素最终可以被分成若干个互不相交的环。

在代码中，可以从一个未访问元素开始，不断执行：

```
current = next[current];
```

直到回到已经访问过的元素。

```c++
5 3  //N = 5  K = 3
1 2
3 4
2 3
  
//第一次交换
visited[1] = {1,2}
visited[2] = {2,1}
visited[3] = {3}
visited[4] = {4}
visited[5] = {5}

//第二次交换
visited[1] = {1,2}
visited[2] = {2,1}
visited[3] = {3,4}
visited[4] = {4,3}
visited[5] = {5}

//第三次交换
visited[1] = {1,2,3}
visited[2] = {2,1}
visited[3] = {3,4}
visited[4] = {4,3,2}
visited[5] = {5}

//奶牛最终在的位置
nextCow[1] = 3;
nextCow[3] = 4;
nextCow[4] = 2;
nextCow[2] = 1;
nextCow[5] = 5;
```